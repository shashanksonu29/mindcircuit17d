pipeline {
    agent any

    tools {
        maven 'maven_3.9.16'
    }

    stages {

        stage('Checkout') {
            steps {
                echo 'Checkout Git repo'
                git branch: 'main',
                    url: 'https://github.com/shashanksonu29/mindcircuit17d.git'   // Application code repo
            }
        }

     stage('Build & Unit Test') {
            steps {
                sh 'mvn clean test'
            }
        }


        // stage('SonarQube Scan') {
        //     steps {
        //         echo 'Scanning project'
        //         sh 'ls -ltr'

        //         sh '''
        //             mvn sonar:sonar \
        //             -Dsonar.host.url=http://54.224.114.172:9000/ \
        //             -Dsonar.token=squ_868dc5049dbe56805d7556513772a5df75d20d32
        //         '''
        //     }
        // }


        stage('SonarQube Scan') {
            steps {
                withSonarQubeEnv('SonarQube'){

                    sh '''
                        mvn clean sonar:sonar
                    '''
                }
            }
        }


        stage('Quality Gate') {
            steps {
                timeout(time: 5, unit: 'MINUTES') {
                    waitForQualityGate abortPipeline: true
                }
            }
        }


        stage('Build Artifact') {
            steps {
                echo 'Build Artifact'
                sh 'mvn clean package'
            }
        }

        stage('Build Docker Image') {
            steps {
                echo 'Build Docker Image'

                sh '''
                    docker build -t sonu171/shashankapp:${BUILD_NUMBER} -f Dockerfile .
                '''
            }
        }

        stage('Scan Docker Image using Trivy') {
            steps {
                echo 'Scanning Image'

                sh '''
                 trivy image --severity CRITICAL --exit-code 1 sonu171/shashankapp:${BUILD_NUMBER}
                 '''
              
            }
        }

        stage('Push to Docker Hub') {
            steps {
                script {
                    withCredentials([string(credentialsId: 'dockerhub', variable: 'dockerhub')]) {

                        sh '''
                            docker login -u sonu171 -p ${dockerhub}
                        '''

                        sh '''
                            docker push sonu171/shashankapp:${BUILD_NUMBER}
                        '''
                    }

                    echo 'Pushed to Docker Hub'
                }
            }
        }

        stage('Update GitOps Repo') {

            environment {
                GIT_REPO_NAME = "mindcircuit17d"
                GIT_USER_NAME = "shashanksonu29"
            }

            steps {
                echo 'Update Deployment File in GitOps Repo'

                withCredentials([string(credentialsId: 'githubtoken', variable: 'githubtoken')]) {

                    sh '''
                        git config user.email "shashasonu29@gmail.com"
                        git config user.name "shashanksonu29"

                        sed -i "s/shashankapp:.*/shashankapp:${BUILD_NUMBER}/g" deploymentfiles/deploy.yaml

                        git add .
                        git commit -m "Update deployment image to version ${BUILD_NUMBER}" || true

                        git push https://${githubtoken}@github.com/${GIT_USER_NAME}/${GIT_REPO_NAME}.git HEAD:main
                    '''
                }
            }
        }
    }
}
